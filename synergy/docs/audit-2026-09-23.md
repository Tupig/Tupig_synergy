# Full Audit Report 2026-09-23 / 全面审计报告 2026-09-23

> **Language / 语言**: [English](#english) | [中文](#中文)
>
> **Last updated / 最后更新**: 2026-09-23
> **Baseline / 审计基线**: branch `main`, HEAD `7bf1325b5`, working tree +3 modified / 2 untracked
> **Scope / 范围**: `synergy/` (code, scripts, docs, triplets), repository-root `.github/`, git process records
> **Related / 相关**: `AGENTS.md`, `docs/HANDOFF.md`, `docs/consistency-audit.md`, `.github/ISSUE_TEMPLATE/security-quality-refactoring.md`
>
> New findings use the `A-nn` numbering. Existing registers stay authoritative for their own
> items: `U-nn` in `docs/consistency-audit.md`, `S/Q/P/T` in the issue tracker. This document is
> a point-in-time snapshot; status changes happen in those registers, not here.
>
> 新发现使用 `A-nn` 编号；既有台账各自保持权威 —— `U-nn` 见 `docs/consistency-audit.md`，
> `S/Q/P/T` 见 issue 追踪文档。本文是时点快照，状态流转在各台账中进行，不在本文原地改写。

---

## English

### Round 0 — Scope, goals, acceptance criteria

**Scope**

| Dimension | Included | Excluded |
|---|---|---|
| Code | `synergy/src/**`, `extra/**`, `cmake/**`, `triplets/**` | `vendor/`, `build/` (R7, verified untracked) |
| Scripts | `setup.bat`, `scripts/*.{bat,sh}` (6 files) | — |
| Docs | `README.md`, `AGENTS.md`, `docs/*.md` (12 files) | archives, spot-checked only |
| Process | git history, `docs/HANDOFF.md`, issue tracker | — |
| CI | repository-root `.github/**` (5 workflows + 8 actions + templates) | — |

**Goals**

1. Verify compliance against `AGENTS.md` R1–R10 and the delivery constraints item by item.
2. Re-verify the *current truth* of the existing registers (`U-01`~`U-21`, 23 `S/Q/P/T` items) instead of re-opening closed work.
3. Surface findings outside those registers, each with evidence, remediation and a verification loop.

**Acceptance criteria (for this report)**

- Every finding carries ≥1 checkable evidence (file:line, command output, or commit id).
- "Fixed" status requires empirical proof (build log, artifact listing, code search) — reasoning alone is insufficient (same rule as `consistency-audit.md`).
- Every remediation names an owner, a deadline and a verification method.
- No collision with existing `U/S/Q/P/T` IDs; new findings are `A-nn`.

**Baseline**: `main` @ `7bf1325b5` (2026-09-23); working tree has uncommitted changes covered by A-09.

---

### Round 1 — Documentation vs code vs registers

**Verified consistent**

- All 9 documents linked from `README.md` exist; no broken doc links found in the sample.
- U-12 preset-name fixes hold: `docs/build.md:527-543` lists the real presets, matching `CMakePresets.json`.
- No `.ps1` anywhere (`git ls-files '*.ps1'` empty) — R1 holds.

**Findings**

| ID | Sev | Finding | Evidence |
|---|---|---|---|
| A-01 | P1 | **Documented CMake floor is too low.** `CMakePresets.json` uses schema `"version": 6`, which requires CMake ≥ 3.25, yet the file self-reports `cmakeMinimumRequired 3.24.0` and six documents promise "3.24+". A user with exactly 3.24 following the docs fails at `cmake --preset`. | `CMakePresets.json:2-6`; preset version map v5→3.24 / v6→3.25; local cmake 4.4.3 lists presets fine; claims in `README.md:23`, `docs/build.md:13`, `docs/architecture.md:77,93`, `docs/contributing.md:99`, `AGENTS.md`, `docs/HANDOFF.md:15` |
| A-02 | P1 | **Qt floor disagrees in three places.** Code enforces 6.4.0; documents claim 6.7+; CI installs 6.9.3/6.10.3/6.11.1. Documents and code cannot both be right. | `CMakeLists.txt:88` vs `README.md:22,73`, `docs/build.md:14,261`, `docs/architecture.md:57`, `docs/HANDOFF.md:15` vs `.github/workflows/ci.yml:147,153,284,292` |
| A-03 | P1 | **`HANDOFF.md` contradicts itself and two sibling registers.** ① Header says "Phase 2 Step 1-3 done"; §5.1 says "Step 3 pending"; §6 says "execute Step 3 next". ② The issue tracker (`:208`) says Step 3 completed on 09-18 — three documents use three different definitions of "Step 3". ③ Header metadata ("last updated 2026-09-21", "latest commit: (pending)") is stale vs HEAD `7bf1325b5`. ④ §4.3 calls `QtNetworkTransport` a "skeleton"; the code is a full implementation (QTcpSocket member, five signal connections, `nextPendingConnection`). | `docs/HANDOFF.md:5-6,152,174`; `security-quality-refactoring.md:208`; `src/lib/net/QtNetworkTransport.cpp:22,42-50,264`; `git log` |
| A-04 | P2 | **Issue tracker out of sync** (violates HANDOFF §1.3). "Last updated 2026-09-21"; changelog ends 09-22 (`:216-217`), while 09-23 alone has 15+ commits (U-02/U-04~U-07/U-12/U-20 fixes, CRT fix, signing script → .bat) unregistered. `:59` still says the consistency audit has "19 items U-01~U-19"; it now has 21. | `security-quality-refactoring.md:7,59,195-217` vs `git log --oneline -15` (all 2026-09-23) |
| A-05 | P2 | **`consistency-audit.md` status column lags its own body.** ① U-16 marked Open, but both CI evidence bullets are obsolete: `ci.yml:671-680` now uses `com.tupig.synergy` throughout, and unversioned-name probes became *correct* after the U-01 fix — only the misplaced issue-template bullet survives. ② The "Verification needed" section (`:349-351`) still says U-01 needs a build, although U-01 is Fixed and `delivery.md:127-130` records an end-to-end handshake test. ③ `:63` still cites a stale branch commit. | `consistency-audit.md:56,63,349-351` vs `.github/workflows/ci.yml:671-680`, `docs/delivery.md:127-130` |
| A-06 | P2 | **`build.sh` points users at a hidden preset.** The debug rejection message suggests "a diagnostics preset such as `linux-asan`", but `linux-asan` is `hidden: true` and cannot be passed to `cmake --preset`; the usable name is `linux-asan-build`. | `scripts/build.sh:37` vs `CMakePresets.json:107-110` (hidden), `:147-151` (visible variant) |
| A-07 | P3 | ① `README.md:1` title repeats English/English ("TuPig Synergy / TuPig Synergy"), breaking R9 bilingual form. ② README quick start (`:106-107`) and `setup.bat:125-127` both teach raw `cmake --preset`, not the declared user-facing entry `scripts\build.bat` — the commands work, but the guidance conflicts with AGENTS' "only two entry points" statement. | `README.md:1,97-108`; `setup.bat:125-127`; `AGENTS.md` build policy |
| A-08 | P3 | **Stale/phantom comments** (new instances for U-08). `Synergy.cmake:30-31` claims the macOS deployment target is also read by `apps/gui-electron/dist/package.config.js` — no `gui-electron` exists anywhere; `:66` claims the version lives in `./VERSION` — no such file (already listed under U-08, still unfixed); `:17-18` claims paths are "space-free" while `"TuPig Synergy"` contains a space. | `extra/cmake/Synergy.cmake:17-18,30-31,66`; globs `**/gui-electron/**` and `**/VERSION` both empty |
| A-10 | P3 | `IDataSocket` still has `assert(0 && "bad call")` at its two abstract entry points. Programmer-contract asserts (not the S-3 network-input class); no-ops in Release. Coexists with "Q-5 fixed" — logged as an observation, not a defect to fix now. | `src/lib/net/IDataSocket.cpp:19,25` |
| A-11 | P3 | Broken relative link in the issue tracker: `../../docs/consistency-audit.md` resolves to repository-root `docs/`, which does not exist (the file lives at `synergy/docs/`). Found while registering this report. | `.github/ISSUE_TEMPLATE/security-quality-refactoring.md:59`; `Test-Path D:\TuPig_Product\docs` → False |

**Existing open items re-verified (not re-opened, current evidence)**

| ID | Sev | Current evidence |
|---|---|---|
| U-03 | P1 | `deploy/linux/org.deskflow.{desktop,metainfo.xml,png}` + `flatpak/org.deskflow.deskflow.yml` still present (glob) |
| U-10 | P3 | `extra/deploy/linux/com.tupig.synergy.metainfo.xml:9` still `<developer id="com.symless">` |
| U-13 | P2 | `1.21.2` hardcoded at 7 sites: `vcpkg.json:4`, `HANDOFF.md:16`, `protocol.md:38,255`, `troubleshooting.md:320,639`, `delivery.md:117,216` (last two are example filenames — tolerable) |
| U-15 | P2 | `vcpkg.json:8` is `GPL-2.0-only`, missing `WITH LicenseRef-OpenSSL-Exception` (SPDX headers and `PKGBUILD.in:12` carry the exception; README states plain GPLv2) |
| U-07 / U-17 / U-18 / U-19 | P2–P3 | No fixing commits since; U-17's `TuPig Synergy 1` confirmed at `Synergy.cmake:23` |

---

### Round 2 — Build scripts & dependencies vs AGENTS R1–R10

| Rule | Result | Evidence |
|---|---|---|
| R1 `.bat` only, no `.ps1` | PASS | `git ls-files '*.ps1'` empty; signing script converted in `ff462b55b` (`scripts/ci-windows-signing.bat`) |
| R2 invokable from cmd and PowerShell | PASS | All 6 scripts self-locate (`%~dp0` / `BASH_SOURCE`), use `setlocal` / `set -euo pipefail`; headers state compatibility (`build.bat:20-21`) |
| R3 ASCII-only script text | PASS | Byte-level check of all 6 scripts: zero bytes > 127 |
| R4 no machine-specific paths/env | PASS | No `VCPKG_ROOT` dependency; `build.bat:79-81` fallback to the canonical `Program Files (x86)` location is commented (`:71-77`) |
| R5 vcpkg manifest + documented CI exception | PASS | `vcpkg.json` deps = openssl/qtbase/qtsvg/qttools/qttranslations; CI exception implemented by root `.github/actions/install-dependencies` |
| R6 baseline single source | PASS | `vcpkg.json:5`; read by `bootstrap-vcpkg.bat:35-44` and `.sh:31-37`; no duplicates |
| R7 `vendor/`, `build/` never committed | PASS | `git ls-files build vendor` empty; `.gitignore:10-17` |
| R8 three-platform source tree | STRUCTURE PASS, VERIFICATION GAP | `src/lib/platform/{win32,linux,macos}/` correct; `delivery.md:134-136` states macOS/Linux builds have **never been verified** |
| R9 bilingual single-file docs | PASS (documented exceptions) | Sectioned `## English` + `## 中文` throughout; 3 operational files exempt per `consistency-audit.md:197-199` |
| R10 Chinese commit messages | PASS | Last 15 commits: Chinese body + English scope prefix (`git log`) |

Script-quality highlights (no finding, recorded so the next round does not re-open): vswhere self-location, mandatory `vcvarsall` activation (`build.bat:99-114`, must not be removed per AGENTS), self-explaining debug rejection, shallow-clone baseline backfill (`bootstrap-vcpkg.{bat,sh}:45-50`).

A-06 (hidden-preset hint) is the only new script finding; it is recorded in Round 1.

---

### Round 3 — Source baseline & process discipline

**Security baseline spot-checks (S-1…S-5)**

| Item | Result | Evidence |
|---|---|---|
| S-1 TLS verification | Landed | `SecureSocket.cpp:63` documents the replacement of `verifyIgnoreCertCallback`; no live occurrences of that symbol |
| S-3 assert→exception | Landed (scope = format-specifier asserts) | Remaining `ProtocolUtil.cpp` asserts are null preconditions (`:72-73,109-110,133-134,452-453`); all 15 format-specifier asserts gone; no `assert(0)` hits |
| S-4 InputValidator | Landed with tests | `InputValidatorTests.cpp` exists; tracker `:216` records wiring into ServerProxy inbound path |
| Hardcoded machine paths in `src/` | None | All `C:\` hits are unit-test sample data (`Win32DropDataTests.cpp` etc.) |

**Process finding**

| ID | Sev | Finding | Evidence |
|---|---|---|---|
| A-09 | P2 | **Uncommitted WIP violates "commit each logical change immediately"** (AGENTS workflow §2). 3 modified files (`platform/CMakeLists.txt` adds `MSWindowsDropTarget` sources + `ole32`/`shell32` link; `MSWindowsScreen.{h,cpp}` +64 lines) plus **2 untracked new files** `MSWindowsDropTarget.{cpp,h}`. Untracked files can be lost; stacking days of work also breaks one-change-per-commit. This is Windows drag-drop step 2, following committed step 1 (`7bf1325b5`). | `git status --porcelain`; `git diff --stat` (+64); `git log` |

A-10 (contract asserts) is recorded in Round 1.

---

### Round 4 — CI/CD & delivery constraints

**Verified consistent**

- CI anchoring fixed for good: all 5 workflows use `working-directory: synergy` with `synergy/` path filters (`ci.yml:59-65` and siblings), matching commit `0a777900a` and the header note in `consistency-audit.md`.
- CI Qt versions 6.9.3 / 6.10.3 / 6.11.1 ≥ `REQUIRED_QT_VERSION` — satisfies AGENTS R5 obligation #1.
- U-16's CI-identity bullets are fixed: `ci.yml:671-680` uses `com.tupig.synergy` for lint/manifest (only the status column lags — A-05).
- U-01 fix is proven end-to-end: `delivery.md:127-130` records a live GUI→core handshake; `dumpbin /DEPENDENTS` shows no MSVC CRT; 25/25 tests in Release and ASan.
- Delivery constraints (separate daemon, portable package without daemon, macOS `.app`, no self-install option) agree across `delivery.md`, AGENTS "do not fix these", and the code: `rg 'CreateService|install-service' src/` has zero hits.

**Findings**

| ID | Sev | Finding | Evidence |
|---|---|---|---|
| R8 gap | P1 risk | **macOS/Linux delivery has zero verification**: no machine for either platform; DEB/RPM/DMG never produced; U-02/U-04/U-06 fixes are code-level reasoning only. | `delivery.md:25-26,134-136` (self-declared); `consistency-audit.md:349-351` |
| A-04 | P2 | CI-relevant commits of 09-23 (`0a777900a`, `ff462b55b`, …) unregistered in the tracker. | see A-04 |

---

### Consolidated findings

| ID | Sev | Summary | Register |
|---|---|---|---|
| A-01 | P1 | CMake "3.24+" claim < actual presets-v6 requirement (3.25) | new |
| A-02 | P1 | Qt floor inconsistent: 6.4.0 (code) / 6.7 (docs) / 6.9+ (CI) | new |
| A-03 | P1 | HANDOFF self-contradiction + three-way "Step 3" drift + stale header | U-14 hardened |
| U-03 | P1 | Three product identities; `org.deskflow.*` ×4 files remain | existing Open |
| A-09 | P2 | WIP uncommitted (2 untracked source files at risk) | new |
| A-04 | P2 | Issue tracker 15+ commits behind; U-count stale | new (breach of §1.3) |
| A-05 | P2 | consistency-audit status column / verification section stale | new |
| A-06 | P2 | build.sh suggests hidden preset `linux-asan` | new |
| U-13 | P2 | Version `1.21.2` hardcoded in 7 places | existing Open |
| U-15 | P2 | License string missing OpenSSL exception | existing Open |
| U-07 | P2 | Two icon themes linked into one binary | existing Open |
| U-08 / A-08 | P3 | Comments contradict code (incl. phantom `gui-electron` path) | existing + new |
| U-10 | P3 | metainfo developer still `com.symless` | existing Open |
| U-17/18/19 | P3 | Display names / deskflow boundary / test placement | existing Open |
| A-07 | P3 | README title not bilingual; entry-point guidance vs AGENTS | new |
| A-10 | P3 | `IDataSocket` contract `assert(0)` remains | new (watch item) |
| A-11 | P3 | Broken relative link in issue tracker | new |

---

### Remediation plan

Deadlines use **T = 2026-09-23**. Roles: **Dev** = primary developer, **Agent** = executing AI agent (bound by AGENTS "propose first"), **TBD** = to be assigned by the project owner.

#### Tier 1 — P1, due T+3

| ID | Action (smallest correct change) | Owner | Due | Drive |
|---|---|---|---|---|
| A-01 | Pick one: ① raise every "3.24" claim and `cmakeMinimumRequired` to 3.25 (recommended: local toolchains are ≥3.25, RHEL configures without presets); ② or drop presets schema to v5 after confirming no v6-only field is in use. | Agent proposes → Dev decides → Agent executes | plan T+1, merged T+2 | one commit for all version claims; `rg "3\.24"` sweep |
| A-02 | Choose the single enforcement point (`CMakeLists.txt:88`) and one true floor — 6.4 (conservative) or 6.7 (matches all documents, recommended if the vcpkg baseline Qt ≥ 6.7) — then align README/build.md/architecture/HANDOFF. | Dev decides, Agent executes | T+2 | commit message states the decision; update AGENTS R5 note if floor changes |
| A-03 | Rewrite HANDOFF header + §5/§6 against the code: `QtNetworkTransport` fully integrated; verify the transport-factory default before stating `TCPSocket` replacement status; unify the "Step N" definition table across HANDOFF and the issue tracker; refresh header commit id. | Dev reviews, Agent executes | T+2 | HANDOFF records this report's commit id, self-certifying the sync |
| U-03 | Decide the canonical identity (recommend `com.tupig.synergy`, continuing U-02); remove or relocate `deploy/linux/org.deskflow.*` (4 files); check `src/apps/res/deskflow.qrc` references. | Dev decides, Agent executes | T+3 | `rg org.deskflow` across the tree shows only the intentional upstream-sync allowlist |

#### Tier 2 — P2, due T+7

| ID | Action | Owner | Due | Verification |
|---|---|---|---|---|
| A-09 | Review drag-drop step-2 WIP; if complete, split into two commits (CMake wiring / Screen integration) per one-change-per-commit; if not, record the hold reason in HANDOFF. | Dev | **T+1 (loss prevention, most urgent)** | `git status --porcelain` clean or explicitly recorded |
| A-04 | Backfill the tracker changelog with all 09-22/09-23 commits; correct "U-01~U-19" → "U-01~U-21"; refresh "last updated". | Agent | T+3 | `git log --since=2026-09-22` matches changelog row-for-row |
| A-05 | Narrow U-16 to the surviving bullet (template placement); retire the stale "verification needed" text; archive the `:63` branch note. | Agent | T+3 | status table self-consistent with detail sections; no dangling "needs verification" for U-01 |
| A-06 | `build.sh:37`: suggest `linux-asan-build` (or list all three `*-build` names). | Agent | T+3 | `bash -n scripts/build.sh`; names match visible presets |
| U-13 | Converge versions: add a CI check comparing `vcpkg.json` `version-string` against `Version.cmake`; rewrite doc hardcodes to reference `--version` where practical. | Agent proposes | T+7 | 10-line comparison script fails CI on mismatch |
| U-15 | Set `vcpkg.json` license to `GPL-2.0-only WITH LicenseRef-OpenSSL-Exception`; align README license section. | Dev confirms legal wording, Agent executes | T+5 | `rg GPL vcpkg.json README.md PKGBUILD.in` consistent |
| U-07 | Decide: drop the upstream `deskflow.qrc` icon theme (check upstream-sync policy first) or keep intentionally and record it as designed. | Dev decides | T+7 | built qrc holds one theme, or the decision is recorded in AGENTS/delivery |

#### Tier 3 — P3 + remaining Open, due T+14

| ID | Action | Owner | Due |
|---|---|---|---|
| U-08 / A-08 | Fix 4 comments: remove phantom `gui-electron` path; `VERSION` → `Version.cmake`; rewrite "space-free" claim; refresh `UrlConstants.h` example. | Agent | T+10 |
| U-10 | Decide metainfo developer/homepage: keep as upstream attribution (with an explanatory comment) or replace with TuPig. | Dev | T+10 |
| A-07 | Bilingual README title; README/setup.bat closing hints lead with `scripts\build.bat release`, raw cmake marked as equivalent advanced usage. | Agent | T+7 |
| U-17/18/19 | U-17: unify display-name decision. U-18: any rename must touch `.ts` files + `kUpstreamId` + `I18NTests` together — add a CI guard first. U-19: move tests to consistent homes. | Dev decides, Agent executes | T+14 |
| A-10 | Watch item — no change until a misuse incident forces it. | — | parked |
| R8 gap | Treat green macOS/Linux CI legs as the first real verification step; packaging verification waits for machines. | Dev monitors CI | T+14 |

**Execution discipline** (inherited from AGENTS, no new process):

1. Propose before changing; smallest correct change wins — especially for the five **decision-required** items (A-01, A-02, U-03, U-07, U-10).
2. One logical change per commit, committed immediately (A-09 is the current counter-example; clear it first).
3. Every remediation commit must update `docs/HANDOFF.md` **and** the issue tracker (A-04 is the current counter-example).
4. Closing an item requires evidence; never flip status on reasoning alone.

---

### Verification methods & follow-up

**Per-change verification (copy-pasteable)**

| Class | Command / method | Pass criterion |
|---|---|---|
| Doc claims (A-01, A-02, U-13, U-15) | `rg -n '3\.24\|6\.7\|6\.4\|1\.21\.2\|GPL-2' README.md docs/ vcpkg.json CMakeLists.txt` | Claims match their single source; zero conflicting hits |
| Register sync (A-03, A-04, A-05) | `git log --since=<date>` vs HANDOFF + tracker changelog | Every commit has a row in both registers |
| Scripts (A-06) | `bash -n scripts/build.sh`; `cmake --list-presets` | Suggested names are visible presets |
| Identity (U-03, U-07, U-10) | `rg -n 'org\.deskflow\|com\.symless' --glob '!vendor/**'` | Only allowlisted upstream-sync files remain |
| Build / delivery | `scripts\build.bat release` + `--target package` + delivery.md checklist | Three binaries + portable archive; GUI→core handshake re-test |
| WIP (A-09) | `git status --porcelain` | No untracked sources |

**Follow-up mechanism**

1. **Register routing**: A-01…A-11 live in `.github/ISSUE_TEMPLATE/security-quality-refactoring.md` §2.4; U items stay in `consistency-audit.md`. No third register.
2. **Pre-commit three questions** (candidates for the PR template): HANDOFF updated? Tracker updated? Evidence attached to any status flip?
3. **Rotating audit rounds** (each round keeps scope → evidence → remediation → verification):
   - Round 5: security deep-dive — TLS fingerprint TOFU, InputValidator coverage gaps (mouse/modifier bypass), file-transfer on-disk hardening (`FileTransferPath`) re-test.
   - Round 6: test coverage vs Q-6 (<30%) and the current 25-test suite; per-module gap list.
   - Round 7: performance — verify P-2…P-5, actually run `linux-tsan` / `linux-coverage` presets.
4. **CI gates**: keep `static-analysis.yml` + `-Werror`; add a cheap `docs-consistency` job running the `rg` comparisons above so A-01/A-02/A-06/U-13/U-15 cannot silently regress.
5. **Re-audit cadence**: after each tier completes, re-check only the closed items of that tier; never re-open entries already listed under "verified consistent".

**Conclusion**: build/dependency/script layer (R1–R7, R10) is compliant and the S-1…S-5 security baseline is demonstrably landed. The dominant risks are three-way drift among documents, code and registers (A-01…A-05, three P1s) plus process discipline (A-09, A-04), with one structural decision outstanding (U-03). Zero macOS/Linux verification is the largest delivery blind spot and is tracked under R8.

---

## 中文

### 第 0 轮：审查范围、目标与验收标准

**范围**

| 维度 | 纳入 | 排除 |
|---|---|---|
| 代码 | `synergy/src/**`、`extra/**`、`cmake/**`、`triplets/**` | `vendor/`、`build/`（R7，已核实未被跟踪） |
| 脚本 | `setup.bat`、`scripts/*.{bat,sh}`（6 个） | — |
| 文档 | `README.md`、`AGENTS.md`、`docs/*.md`（12 份） | 归档目录仅抽查 |
| 流程 | git 提交记录、`docs/HANDOFF.md`、issue 追踪文档 | — |
| CI | 仓库根 `.github/**`（5 workflow + 8 action + 模板） | — |

**目标**

1. 对照 `AGENTS.md` R1–R10 与交付约束逐条核查合规性；
2. 复核既有台账（`U-01`~`U-21`、23 项 `S/Q/P/T`）的**当前真实性**，不重复开单；
3. 发现台账之外的问题，每条附证据、整改与验证闭环。

**验收标准（对本报告自身）**

- 每条问题 ≥1 项可复核证据（文件:行号、命令输出或提交号）；
- 判「已修复」必须有实证（构建日志、产物清单、代码检索），仅凭推理不算（同 `consistency-audit.md` 规则）；
- 每条整改含责任角色、时限、验证方法；
- 不与既有 `U/S/Q/P/T` 编号冲突，新问题用 `A-nn`。

**基线**：`main` @ `7bf1325b5`（2026-09-23）；工作区未提交改动见 A-09。

---

### 第 1 轮：文档 ↔ 代码 ↔ 台账对账

**已核实一致**

- README 引用的 9 份文档全部存在，抽查无断链；
- U-12 预设名已修正且与 `CMakePresets.json` 一致（`docs/build.md:527-543`）；
- 全仓库无 `.ps1`（`git ls-files '*.ps1'` 空）——R1 成立。

**发现的问题**

| ID | 级别 | 问题 | 证据 |
|---|---|---|---|
| A-01 | P1 | **CMake 最低版本声明不足**：`CMakePresets.json` 用 schema `"version": 6`（需 CMake ≥3.25），文件却自报 `cmakeMinimumRequired 3.24.0`，另有六处文档宣称「3.24+」。恰好装 3.24 的用户按文档操作会在 `cmake --preset` 处失败。 | `CMakePresets.json:2-6`；presets 版本映射 v5→3.24 / v6→3.25；本机 cmake 4.4.3 列预设正常；声明见 `README.md:23`、`docs/build.md:13`、`docs/architecture.md:77,93`、`docs/contributing.md:99`、`AGENTS.md`、`docs/HANDOFF.md:15` |
| A-02 | P1 | **Qt 下限三方不一致**：代码强制 6.4.0，文档宣称 6.7+，CI 装 6.9.3/6.10.3/6.11.1。文档与代码必有一错。 | `CMakeLists.txt:88` vs `README.md:22,73`、`docs/build.md:14,261`、`docs/architecture.md:57`、`docs/HANDOFF.md:15` vs `.github/workflows/ci.yml:147,153,284,292` |
| A-03 | P1 | **HANDOFF 自相矛盾且与另两份台账三方漂移**：①头部称「Phase 2 Step 1-3 已完成」，§5.1 称「Step 3 待实施」，§6 又称「立即执行 Step 3」；②issue 追踪 `:208` 称 Step 3 已于 09-18 完成——三份文档对「Step 3」定义互不相同；③头部元数据（「最后更新 2026-09-21」「最新 Commit (待提交)」）落后于 HEAD `7bf1325b5`；④§4.3 称 `QtNetworkTransport` 是「骨架」，代码实为完整实现（QTcpSocket 成员、5 个信号连接、`nextPendingConnection`）。 | `docs/HANDOFF.md:5-6,152,174`；`security-quality-refactoring.md:208`；`src/lib/net/QtNetworkTransport.cpp:22,42-50,264`；`git log` |
| A-04 | P2 | **Issue 追踪脱节**（违反 HANDOFF §1.3）：「最后更新 2026-09-21」，变更日志止于 09-22（`:216-217`），而 09-23 单日 15+ 提交（U-02/U-04~U-07/U-12/U-20 修复、CRT 修复、签名 .bat 化等）全部未登记；`:59` 仍称一致性审计「共 19 项 U-01~U-19」，实际已 21 项。 | `security-quality-refactoring.md:7,59,195-217` vs `git log --oneline -15`（全部 2026-09-23） |
| A-05 | P2 | **`consistency-audit.md` 状态栏滞后于自身正文**：①U-16 标 Open，但两条 CI 证据均已过时——`ci.yml:671-680` 已全用 `com.tupig.synergy`，无版本名探测在 U-01 修复后反而是正确行为，仅剩「模板位置不当」一条；②「待验证项」（`:349-351`）仍称 U-01 需构建验证，但 U-01 已 Fixed 且 `delivery.md:127-130` 记录了端到端握手实测；③`:63` 仍引用过时分支提交。 | `consistency-audit.md:56,63,349-351` vs `.github/workflows/ci.yml:671-680`、`docs/delivery.md:127-130` |
| A-06 | P2 | **build.sh 指向隐藏预设**：debug 拒绝信息建议「diagnostics preset such as `linux-asan`」，但 `linux-asan` 为 `hidden: true`，不能直接传给 `cmake --preset`；可用名是 `linux-asan-build`。 | `scripts/build.sh:37` vs `CMakePresets.json:107-110`（hidden）、`:147-151`（可见变体） |
| A-07 | P3 | ①`README.md:1` 标题英文/英文重复（「TuPig Synergy / TuPig Synergy」），违反 R9 双语格式；②README 快速开始（`:106-107`）与 `setup.bat:125-127` 均教裸 `cmake --preset`，而非 AGENTS 声明的官方入口 `scripts\build.bat`——命令可用，但与「用户可见入口只有两个」的表述冲突。 | `README.md:1,97-108`；`setup.bat:125-127`；`AGENTS.md` 构建策略节 |
| A-08 | P3 | **注释陈旧/幽灵路径**（U-08 新增实例）：`Synergy.cmake:30-31` 称 macOS 最低版本同时由 `apps/gui-electron/dist/package.config.js` 读取——全库无 `gui-electron`；`:66` 称版本在 `./VERSION`——无此文件（U-08 已列，仍未修）；`:17-18` 称「space-free」但 `"TuPig Synergy"` 含空格。 | `extra/cmake/Synergy.cmake:17-18,30-31,66`；glob `**/gui-electron/**` 与 `**/VERSION` 均空 |
| A-10 | P3 | `IDataSocket` 两个抽象入口仍为 `assert(0 && "bad call")`。属程序员契约断言（非 S-3 网络输入类），Release 下 no-op；与「Q-5 已修复」口径并存但未登记——记为观察项，暂不修。 | `src/lib/net/IDataSocket.cpp:19,25` |
| A-11 | P3 | issue 追踪相对链接失效：`../../docs/consistency-audit.md` 解析到仓库根 `docs/`（不存在，实际在 `synergy/docs/`）。登记本报告时发现。 | `security-quality-refactoring.md:59`；`Test-Path D:\TuPig_Product\docs` → False |

**既有 Open 项复核（维持 Open，附当前证据）**

| ID | 级别 | 当前证据 |
|---|---|---|
| U-03 | P1 | `deploy/linux/org.deskflow.{desktop,metainfo.xml,png}` + `flatpak/org.deskflow.deskflow.yml` 共 4 文件仍在（glob 实证） |
| U-10 | P3 | `extra/deploy/linux/com.tupig.synergy.metainfo.xml:9` 仍 `<developer id="com.symless">` |
| U-13 | P2 | `1.21.2` 硬编码 7 处：`vcpkg.json:4`、`HANDOFF.md:16`、`protocol.md:38,255`、`troubleshooting.md:320,639`、`delivery.md:117,216`（后两处为示例文件名，可容忍） |
| U-15 | P2 | `vcpkg.json:8` 为 `GPL-2.0-only`，缺 `WITH LicenseRef-OpenSSL-Exception`（SPDX 头与 `PKGBUILD.in:12` 均带 exception；README 只写 GPLv2） |
| U-07 / U-17 / U-18 / U-19 | P2–P3 | 此后无修复提交；U-17 的 `TuPig Synergy 1` 实证于 `Synergy.cmake:23` |

---

### 第 2 轮：构建脚本与依赖 —— 对照 AGENTS R1–R10

| 规则 | 结论 | 证据 |
|---|---|---|
| R1 仅 .bat、禁 .ps1 | 通过 | `git ls-files '*.ps1'` 空；签名脚本已经 `ff462b55b` 转为 `scripts/ci-windows-signing.bat` |
| R2 cmd/PowerShell 双宿主可调 | 通过 | 6 个脚本均 `%~dp0`/`BASH_SOURCE` 自定位、`setlocal`/`set -euo pipefail`；头部声明兼容性（`build.bat:20-21`） |
| R3 脚本纯 ASCII | 通过 | 6 个脚本逐字节校验，>127 字节为 0 |
| R4 无机器专属路径/环境变量 | 通过 | 无 `VCPKG_ROOT` 依赖；`build.bat:79-81` 回退到标准 `Program Files (x86)` 位置且有注释（`:71-77`） |
| R5 vcpkg manifest + CI 例外 | 通过 | `vcpkg.json` 依赖 = openssl/qtbase/qtsvg/qttools/qttranslations；CI 例外由根 `.github/actions/install-dependencies` 承担 |
| R6 baseline 单一真源 | 通过 | `vcpkg.json:5`；`bootstrap-vcpkg.bat:35-44` 与 `.sh:31-37` 均从此读取，无副本 |
| R7 vendor/build 不入库 | 通过 | `git ls-files build vendor` 空；`.gitignore:10-17` |
| R8 三平台源码树 | 结构通过，**验证缺口** | `src/lib/platform/{win32,linux,macos}/` 正确；`delivery.md:134-136` 自述 macOS/Linux 构建**从未验证** |
| R9 单文件双语文档 | 通过（例外已备案） | 主文档均为 `## English` + `## 中文` 分节；3 个操作类文件豁免见 `consistency-audit.md:197-199` |
| R10 中文提交信息 | 通过 | 近 15 条提交：中文正文 + 英文 scope 前缀（`git log`） |

脚本质量记录（非问题，供下轮免重复开单）：vswhere 自定位、强制 `vcvarsall` 激活（`build.bat:99-114`，AGENTS 明令不可删）、Debug 拒绝信息自解释、baseline 浅克隆补 fetch（`bootstrap-vcpkg.{bat,sh}:45-50`）。

本节唯一新发现为 A-06（隐藏预设提示），已计入第 1 轮表。

---

### 第 3 轮：源码基线与过程纪律

**安全基线抽查（S-1…S-5）**

| 项 | 结论 | 证据 |
|---|---|---|
| S-1 TLS 验证 | 已落地 | `SecureSocket.cpp:63` 注释确认替换 `verifyIgnoreCertCallback`；该符号无存活引用 |
| S-3 assert→异常 | 已落地（口径=格式符 assert） | `ProtocolUtil.cpp` 残留均为判空前置（`:72-73,109-110,133-134,452-453`）；15 处格式符 assert 全部消失；无 `assert(0)` 命中 |
| S-4 InputValidator | 已落地且有测试 | `InputValidatorTests.cpp` 存在；追踪 `:216` 记录已接入 ServerProxy 入站路径 |
| `src/` 内机器硬编码路径 | 无 | `C:\` 命中全部为单元测试样本数据（`Win32DropDataTests.cpp` 等） |

**过程发现**

| ID | 级别 | 问题 | 证据 |
|---|---|---|---|
| A-09 | P2 | **未提交 WIP 违反「一提交一事、做完立即提交」**（AGENTS 流程 §2）：3 个修改（`platform/CMakeLists.txt` 增 `MSWindowsDropTarget` 源与 `ole32`/`shell32` 链接、`MSWindowsScreen.{h,cpp}` +64 行）+ **2 个未跟踪新文件** `MSWindowsDropTarget.{cpp,h}`。未跟踪文件可能被误删；多日堆叠也破坏提交粒度。属 Windows 拖拽第 2 步，第 1 步已提交（`7bf1325b5`）。 | `git status --porcelain`；`git diff --stat`（+64）；`git log` |

A-10（契约断言）已计入第 1 轮表。

---

### 第 4 轮：CI/CD 与交付约束

**已核实一致**

- CI 锚定问题已根治：5 个 workflow 均 `working-directory: synergy` + `synergy/` 路径过滤（`ci.yml:59-65` 及同级文件），与提交 `0a777900a`、`consistency-audit.md` 头注一致；
- CI Qt 6.9.3 / 6.10.3 / 6.11.1 ≥ `REQUIRED_QT_VERSION`，满足 AGENTS R5 义务第 1 条；
- U-16 的 CI 身份两条已修：`ci.yml:671-680` 的 lint/manifest 全部 `com.tupig.synergy`（仅状态栏滞后——A-05）；
- U-01 修复已端到端实证：`delivery.md:127-130` 记录 GUI→core 握手实测；`dumpbin /DEPENDENTS` 无 MSVC CRT；Release 与 ASan 各 25/25 测试通过；
- 交付约束（daemon 独立、便携包排除 daemon、macOS `.app`、无自安装服务）在 `delivery.md`、AGENTS「不要修复这些」与代码三方一致：`rg 'CreateService|install-service' src/` 零命中。

**发现的问题**

| ID | 级别 | 问题 | 证据 |
|---|---|---|---|
| R8 缺口 | P1 风险 | **macOS/Linux 交付零验证**：无对应机器；DEB/RPM/DMG 从未产出；U-02/U-04/U-06 的修复只有代码级推理。 | `delivery.md:25-26,134-136`（自述）；`consistency-audit.md:349-351` |
| A-04 | P2 | 09-23 与 CI 相关的提交（`0a777900a`、`ff462b55b` 等）未登记进追踪文档。 | 见 A-04 |

---

### 问题总表

| ID | 级别 | 摘要 | 台账归属 |
|---|---|---|---|
| A-01 | P1 | CMake「3.24+」声明低于 presets v6 实际所需 3.25 | 新发现 |
| A-02 | P1 | Qt 下限 6.4.0（代码）/ 6.7（文档）/ 6.9+（CI）三方不一致 | 新发现 |
| A-03 | P1 | HANDOFF 自相矛盾 + 「Step 3」三方定义漂移 + 头部过期 | U-14 加重 |
| U-03 | P1 | 三套产品身份；`org.deskflow.*` ×4 文件残留 | 既有 Open |
| A-09 | P2 | WIP 未提交（2 个 untracked 源文件有丢失风险） | 新发现 |
| A-04 | P2 | Issue 追踪落后 15+ 提交；U 计数过期 | 新发现（违反 §1.3） |
| A-05 | P2 | consistency-audit 状态栏/待验证节滞后 | 新发现 |
| A-06 | P2 | build.sh 建议使用隐藏预设 `linux-asan` | 新发现 |
| U-13 | P2 | 版本号 `1.21.2` 硬编码 7 处 | 既有 Open |
| U-15 | P2 | 许可声明缺 OpenSSL exception | 既有 Open |
| U-07 | P2 | 两套图标主题编进同一二进制 | 既有 Open |
| U-08 / A-08 | P3 | 注释与代码相反（含 `gui-electron` 幽灵路径） | 既有 + 新增 |
| U-10 | P3 | metainfo 供应商仍为 `com.symless` | 既有 Open |
| U-17/18/19 | P3 | 显示名 / deskflow 边界 / 测试归属 | 既有 Open |
| A-07 | P3 | README 标题非双语；入口指引与 AGENTS 不一致 | 新发现 |
| A-10 | P3 | `IDataSocket` 契约 `assert(0)` 残留 | 新发现（观察项） |
| A-11 | P3 | issue 追踪相对链接失效 | 新发现 |

---

### 整改计划

时限以 **T = 2026-09-23** 为基准。角色：**Dev**=主开发者、**Agent**=执行型 AI 代理（受 AGENTS「先方案后改动」约束）、**TBD**=待项目负责人指派。

#### 第一梯队 —— P1，T+3 完成

| ID | 措施（最小正确改动） | 责任 | 时限 | 推进方式 |
|---|---|---|---|---|
| A-01 | 二选一：①所有「3.24」声明与 `cmakeMinimumRequired` 升到 3.25（**推荐**：本地工具链均 ≥3.25，RHEL 走裸 cmake 不用 presets）；②或把 presets 降为 schema v5，前提是确认未用到 v6 专属字段。 | Agent 出方案 → Dev 拍板 → Agent 执行 | 方案 T+1，合入 T+2 | 一次提交统一全部版本声明；`rg "3\.24"` 清扫残留 |
| A-02 | 定唯一强制点（`CMakeLists.txt:88`）与唯一下限——6.4（保守）或 6.7（与全部文档一致，**若 vcpkg baseline Qt ≥6.7 则推荐**）——再统一 README/build.md/architecture/HANDOFF 四处。 | Dev 决策，Agent 执行 | T+2 | 提交信息写明决策；下限若变，同步 AGENTS R5 注记 |
| A-03 | 按代码重写 HANDOFF 头部与 §5/§6：`QtNetworkTransport` 已完整集成；`TCPSocket` 替换状态先核实 transport 工厂默认值再如实填写；统一 HANDOFF 与 issue 追踪的「Step N」定义表；刷新头部提交号。 | Dev 复核，Agent 执行 | T+2 | HANDOFF 记录本报告的提交号，形成自证 |
| U-03 | 决策权威身份（建议 `com.tupig.synergy`，延续 U-02 方向）；删除或迁移 `deploy/linux/org.deskflow.*`（4 文件）；核对 `src/apps/res/deskflow.qrc` 引用。 | Dev 决策，Agent 执行 | T+3 | 删除前 `rg org.deskflow` 全库，仅剩有意保留清单 |

#### 第二梯队 —— P2，T+7 完成

| ID | 措施 | 责任 | 时限 | 验证方法 |
|---|---|---|---|---|
| A-09 | 评审拖拽第 2 步 WIP：完成则按「一提交一事」拆 2 笔（CMake 接线 / Screen 集成）；暂不完成则把保留原因写进 HANDOFF。 | Dev | **T+1（防丢失，最急）** | `git status --porcelain` 干净或有明确 WIP 记录 |
| A-04 | 补登 09-22/09-23 全部提交进变更日志；「U-01~U-19」更正为「U-01~U-21」；刷新「最后更新」。 | Agent | T+3 | `git log --since=2026-09-22` 与变更日志逐条对照零遗漏 |
| A-05 | U-16 收窄为仅剩「模板位置」一条并注明其余已修；退役过期「待验证项」文字；归档 `:63` 旧分支说明。 | Agent | T+3 | 状态表与正文自洽；U-01 不再挂「待验证」 |
| A-06 | `build.sh:37` 改为 `linux-asan-build`（或并列三个 `*-build` 名）。 | Agent | T+3 | `bash -n scripts/build.sh`；名字与可见预设一致 |
| U-13 | 版本收敛：CI 增加 `vcpkg.json` `version-string` 与 `Version.cmake` 的比对步；文档硬编码尽量改为指向 `--version`。 | Agent 出方案 | T+7 | 10 行比对脚本在不一致时令 CI 失败 |
| U-15 | `vcpkg.json` license 改为 `GPL-2.0-only WITH LicenseRef-OpenSSL-Exception`；README 许可节对齐。 | Dev 确认法律口径，Agent 执行 | T+5 | `rg GPL vcpkg.json README.md PKGBUILD.in` 三处一致 |
| U-07 | 决策：去掉上游 `deskflow.qrc` 图标主题（先核对上游同步策略）或有意保留并备案为设计决策。 | Dev 决策 | T+7 | 构建后 qrc 仅一套主题，或决策记入 AGENTS/delivery |

#### 第三梯队 —— P3 + 既有 Open，T+14 完成

| ID | 措施 | 责任 | 时限 |
|---|---|---|---|
| U-08 / A-08 | 修 4 处注释：删 `gui-electron` 幽灵路径；`VERSION`→`Version.cmake`；改写「space-free」；刷新 `UrlConstants.h` 示例。 | Agent | T+10 |
| U-10 | 决策 metainfo developer/homepage：保留上游归属（加注释说明）或替换为 TuPig。 | Dev | T+10 |
| A-07 | README `:1` 改双语标题；README/setup.bat 结尾提示以 `scripts\build.bat release` 为主，裸 cmake 标注为等效高级用法。 | Agent | T+7 |
| U-17/18/19 | U-17：统一显示名决策。U-18：任何改名必须同时改 `.ts` 文件 + `kUpstreamId` + `I18NTests`——先加 CI 防呆。U-19：测试目录归位。 | Dev 决策，Agent 执行 | T+14 |
| A-10 | 观察项：暂不改，直到出现误用事故再升格。 | — | 挂账 |
| R8 缺口 | 把 macOS/Linux CI 腿跑绿当作第一级真实验证；打包验证等有对应机器。 | Dev 关注 CI | T+14 |

**执行纪律**（沿用 AGENTS，不另起炉灶）

1. 动手前先出方案，选最小正确改动——尤其 5 个**需决策**项（A-01、A-02、U-03、U-07、U-10）；
2. 一个提交一件事，做完立即提交（A-09 是当前反例，先清账）；
3. 每笔整改提交必须同步 `docs/HANDOFF.md` **与** issue 追踪（A-04 是当前反例）；
4. 关闭条目必须附证据，禁止凭推理翻状态。

---

### 验证方法与后续跟进机制

**逐项验证（可直接复制执行）**

| 类别 | 命令/手段 | 通过标准 |
|---|---|---|
| 文档声明类（A-01、A-02、U-13、U-15） | `rg -n '3\.24\|6\.7\|6\.4\|1\.21\.2\|GPL-2' README.md docs/ vcpkg.json CMakeLists.txt` | 声明与各自真源一致，零冲突命中 |
| 台账同步类（A-03、A-04、A-05） | `git log --since=<日期>` 对照 HANDOFF 与追踪变更日志 | 每笔提交在两份台账都有对应行 |
| 脚本类（A-06） | `bash -n scripts/build.sh`；`cmake --list-presets` | 提示名均为可见预设 |
| 身份类（U-03、U-07、U-10） | `rg -n 'org\.deskflow\|com\.symless' --glob '!vendor/**'` | 仅剩有意保留清单 |
| 构建/交付类 | `scripts\build.bat release` + `--target package` + 对照 delivery.md 清单 | 三件套 + 便携包产出；GUI→core 握手复测 |
| WIP 类（A-09） | `git status --porcelain` | 无 untracked 源文件 |

**后续跟进机制**

1. **台账路由**：A-01…A-11 登记于 `.github/ISSUE_TEMPLATE/security-quality-refactoring.md` §2.4；U 系列继续在 `consistency-audit.md` 流转——不新增第三套编号体系。
2. **提交前三问**（建议进 PR 模板）：HANDOFF 更新了吗？追踪文档更新了吗？状态翻转附证据了吗？
3. **轮换式审计**（每轮仍走 范围→取证→整改→验证 四步）：
   - 第 5 轮：安全纵深——TLS 指纹 TOFU、InputValidator 覆盖缺口（鼠标/修饰键绕过）、文件传输落盘加固（`FileTransferPath`）复测；
   - 第 6 轮：测试覆盖——对齐 Q-6（<30%）与现有 25 用例的差距，按模块列缺口；
   - 第 7 轮：性能——验证 P-2…P-5，实跑 `linux-tsan` / `linux-coverage` 预设。
4. **CI 门禁固化**：保留 `static-analysis.yml` + `-Werror`；新增轻量 `docs-consistency` job 跑上表 `rg` 比对（版本/许可/预设名），把 A-01/A-02/A-06/U-13/U-15 变为机器门禁防复发。
5. **复审节奏**：每梯队完成后只复审该梯队已关闭条目；已列入「已核实一致」的不重开。

**结论**：构建/依赖/脚本层（R1–R7、R10）合规，S-1…S-5 安全基线落地可证；主要风险是文档-代码-台账三方漂移（A-01…A-05，含 3 个 P1）、过程纪律（A-09、A-04）与一个悬置的结构性决策（U-03）。macOS/Linux 零验证是最大交付盲区，按 R8 跟踪。
