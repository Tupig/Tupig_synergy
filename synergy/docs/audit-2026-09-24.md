# Full Audit Report 2026-09-24 / 全面审计报告 2026-09-24

> **Language / 语言**: [English](#english) | [中文](#中文)
>
> **Last updated / 最后更新**: 2026-09-24
> **Baseline / 审计基线**: branch `main`, HEAD `8695555be`, working tree clean
> **Scope / 范围**: `synergy/` (code, scripts, docs, triplets), repository-root `.github/`, git process records
> **Related / 相关**: `AGENTS.md`, `docs/HANDOFF.md`, `docs/consistency-audit.md`, `.github/ISSUE_TEMPLATE/security-quality-refactoring.md`, `docs/audit-2026-09-23.md`
>
> New findings use the `A-nn` numbering, continuing from A-11. Existing registers stay authoritative
> for their own items: `U-nn` in `docs/consistency-audit.md`, `S/Q/P/T` in the issue tracker.
> This document is a point-in-time snapshot; status changes happen in those registers, not here.
> `docs/audit-2026-09-23.md` is likewise a frozen snapshot of yesterday.
>
> 新发现使用 `A-nn` 编号，自 A-11 顺延；既有台账各自保持权威 —— `U-nn` 见
> `docs/consistency-audit.md`，`S/Q/P/T` 见 issue 追踪文档。本文是时点快照，状态流转在各台账中
> 进行，不在本文原地改写。`docs/audit-2026-09-23.md` 同样是昨日的冻结快照。

---

## English

### Round 0 — Method

Four parallel read-only passes, then independent re-verification of every P1/P2 claim against the tree:

| Pass | Focus |
|---|---|
| 1 | Regression sweep of A-01…A-11 and U-01…U-21 |
| 2 | R1–R10 compliance + cross-document reconciliation |
| 3 | Build / CI / source / delivery (new findings `N-nn` → renumbered `A-nn`) |
| 4 | Deep dive on remaining Open U items + same-class gaps (`C-n`) |

Baseline: `git status --porcelain` empty; HEAD `8695555be` (`docs: HANDOFF 同步 B 计划进度与 G1 清单引用`).

### Round 1 — Regression of yesterday's closures

| ID | Verdict | Evidence |
|---|---|---|
| A-01 | **HOLD** | `CMakePresets.json:5` minor=25; `rg "3\.24"` clean except RHEL floor `CMakeLists.txt:9` (recorded decision) |
| A-02 | **HOLD** | `CMakeLists.txt:88` = 6.7.0; no `6.4.0` hits; AGENTS agrees |
| A-06 | **HOLD** | `scripts/build.sh:13-16` points at visible `linux-asan-build` |
| A-08 / U-08 | **HOLD** | `Synergy.cmake` free of `gui-electron`/`./VERSION`/space-free; `UrlConstants.h:15` matches `kAppDomain` |
| A-11 | **HOLD** | Both relative links resolve |
| U-01…U-06 | **HOLD** | Unversioned outputs; `deploy/linux/` only `deploy.cmake` + `arch/PKGBUILD.in`; mac bundle icon correct; dead plist/dmgbuild gone; PKGBUILD conflicts fixed |
| U-16 identity | **HOLD** | `ci.yml` lint/manifest use `com.tupig.synergy` only |
| U-20 | **HOLD** | `install-service` only appears in "does not exist" explanations |
| **A-03 / U-14** | **REGRESS** | `HANDOFF.md:5` still `72734ba05` (7 commits behind); `:78-83` "latest 6" does not match `HEAD~6`; `:128` says "19 项 U-01~U-19" while `:73` says 21 |
| **A-04** | **PARTIAL** | §2.3 fixed (21 items), but changelog missing the 6 commits after `d8512e321` (incl. `cbd2baf39`, `1269e7cb9`) |
| **A-05** | **PARTIAL** | Summary rows fixed, but detail sections still pre-fix: U-07 `:170` "still open", U-09 `:184` / U-10 `:190` "Decision needed", U-17 table `:250-257` lists gone names, U-10 cites deleted `com.symless.synergy.*` paths, `:63` branch note unarchived |
| **U-10** | **PARTIAL** | developer/homepage → TuPig/GitHub; residual: `Synergy App Ltd` SPDX headers (desktop `:2`, metainfo `:2`, flatpak yml `:1`, `PackageFileName.cmake:1`, `codesign.cmake.in:1`), metainfo `:82` still `symless.com` download URL |
| **U-15** | **PARTIAL** | `vcpkg.json`/README/PKGBUILD aligned; metainfo `:7` `project_license` and flatpak yml `:2` SPDX lack the OpenSSL exception |
| **A-10** | **STALE (tracker only)** | Code fixed: `IDataSocket.cpp` throws `SocketException`; tracker `:78` still "👀 挂账" while HANDOFF and `cbd2baf39` say closed |

**HOLD 13 · REGRESS 1 · PARTIAL 4 · STALE 1.** Yesterday's code fixes did not break; today's failures are documentation-sync debt.

### Round 2 — R1–R10

All ten rules **PASS**. Notables: only 4 `.bat` files exist (all ASCII, all `%~dp0`-self-locating); baseline pinned once in `vcpkg.json`; `vendor/`/`build/` untracked; platform dirs complete; bilingual docs OK except the three operational files already recorded under U-11. R2 edge notes: `setup.bat` interactive `pause` on failure paths; `ci-windows-signing.bat` uses cwd-relative `build\bin`.

### Round 3 — New findings (A-12 … A-27)

#### Tier P1

| ID | Finding | Evidence |
|---|---|---|
| **A-12** | **CI quality gate can go green with zero builds.** The `lint-clang` composite action has no `working-directory`, so `find src/` runs at the repo root (where `src/` does not exist) → job fails. All three build jobs and `build-flatpak` declare `needs: lint-clang` → they skip. `ci-passed` does **not** need `lint-clang`, and its `ok()` counts `skipped` as success → branch protection sees green while nothing was built. | `.github/actions/lint-clang/action.yml:12`; `ci.yml:71,79-92,106,131,271,424,648` |
| **A-13** | **build-flatpak path anchoring is inconsistent.** `defaults.run.working-directory: synergy` covers `run` steps only. The `uses:` step `flatpak/flatpak-github-actions/flatpak-builder@v6` resolves `manifest-path: extra/...` from the workspace root (file lives at `synergy/extra/...`). Validate runs under `synergy/` looking for `repo`, while builder output lands at workspace root; Upload expects `${workspace}/synergy/*.flatpak`. At least two of the three anchors are wrong. *Confirm on a live run.* | `ci.yml:63-65,674-695`; `actions/runner` ADR 0549 (defaults do not cross into composite/uses) |

#### Tier P2

| ID | Finding | Evidence |
|---|---|---|
| **A-14** | `push: branches: [beta]` but the repo only has `main` → pushes to main never trigger CI (PR/nightly/dispatch still do). Separately, `static-analysis.yml` is `workflow_dispatch/workflow_call` with **no caller** (ci.yml only calls valgrind), so clang-tidy/cppcheck never run automatically; its header comment "Triggered by ci.yml" is false. | `ci.yml:33-34`; `static-analysis.yml:2,6-8`; `git branch -r` |
| **A-15** | `NetworkTransportFactory` has no production callers (only its own CMake registration). Live path is always legacy `TCPSocketFactory`. Therefore HANDOFF's claimed 3-level rollback is hollow: `USE_LEGACY_NETWORK` is only read inside the unused factory, and CMake option `LEGACY_NETWORK` does not exist anywhere in `*.cmake`/`CMakeLists.txt`. plan-B `:27` admits the factory is unused — HANDOFF `:102` still promises the rollback. | `NetworkTransportFactory.cpp:32-41`; `rg LEGACY_NETWORK --glob '*.{cmake,txt}'` empty; `HANDOFF.md:102,203` vs `plan-B-D1-E2-F1-G1.md:27,31` |
| **A-16** | README feature table claims file drag-drop is **not implemented** ("v1.5 messages stubbed") — reverse-stale: delivery matrix marks protocol/sender/receiver/Windows OLE/macOS as Implemented with unit tests (`587415ef1`, …). | `README.md:42` vs `docs/delivery.md:106-111` |
| **A-17** | README config example uses non-existent keys: `[client] serverHost/serverPort/reconnectInterval/autoConnect`. Real keys are `client/remoteHost`, `core/port`, `client/dynamicConnectionInterval`; there is no autoConnect. The `[server] screens=…` block is the **server config file** syntax, not the settings INI. Copy-paste fails. | `README.md:166-186` vs `Settings.h:37,43,51` and `configuration.md:59-61,177` |
| **A-18** | **A-03/U-14 recurrence.** HANDOFF header commit + "latest 6" block stale; `:128` U-count 19 contradicts `:73` (=21); `:6` says identity debt U-07/09/17/18/19 closed while `:74` and `:185` still list U-07/U-09 as open product decisions (the summary tables in `consistency-audit.md` already say Fixed / By design after `1269e7cb9`). | `HANDOFF.md:5,6,74,78-83,128,185` vs `consistency-audit.md:47,49,57-59` |
| **A-19** | **A-04/A-05 recurrence.** (a) Issue-tracker changelog stops at `d8512e321`; six later commits — including closures `cbd2baf39` (U-10/13/15/A-10) and `1269e7cb9` (U-07/17/18/19) — are unregistered. (b) consistency-audit detail sections contradict their own summary rows (see Round 1 A-05 row for line numbers). | `security-quality-refactoring.md` §5; `consistency-audit.md:170,184,190,250-257,263,267-270` |

#### Tier P3

| ID | Finding | Evidence |
|---|---|---|
| **A-20** | GoogleTest ghost: README tree, build.md (×2), architecture ADR-0009 (×2), contributing (×2) all say GoogleTest; the suite links `Qt::Test` and gtest appears nowhere. | `src/unittests/CMakeLists.txt:37` vs 7 doc sites |
| **A-21** | CMake defects batch: (1) `if(!X11_xkbfile_FOUND)` — no `!` operator, condition never true, FATAL check dead **and** the variable is never set; (2) multi-config generators: empty `CMAKE_BUILD_TYPE` makes `NOT … STREQUAL "Debug"` true → `NDEBUG` forced even for `--config Debug`; (3) `generate_app_man` body uses `${target}` not `${TARGET}`; (4) `set(${PORTABLE_LIBS} "")` expands to `set("" "")` (undefined var); (5) `SKIP_BUILD_TESTS` default ON in `Synergy.cmake:52` vs OFF claimed in build.md and a second `option(… OFF)`; (6) build.md libportal 0.9.1+/libei 1.3+ vs code 0.8.0/1.0; (7) orphan comment line `build.sh:17`. | `cmake/Libraries.cmake:271`; `CMakeLists.txt:320-323`; `src/apps/CMakeLists.txt:13-31`; `deploy/windows/pre-cpack.cmake.in:6`; `extra/cmake/Synergy.cmake:52`; `src/unittests/CMakeLists.txt:108`; `build.md:16-17,38,283`; `scripts/build.sh:17` |
| **A-22** | Doc dead-links / ghosts: `build.md` cross-compile cites missing `cmake/mingw-toolchain.cmake`; `contributing.md` cites missing `.pre-commit-config.yaml` (×2); issue links use `bug_report.md` but template is `bug_report.yml` (and `blank_issues_enabled: false`); `security.md` demands email disclosure but lists no address; ADR-0011 docs tree lists 7 of 12 files; `build.md:506-507` documents `build.bat\|build.sh [release\|debug]` while both scripts **reject** debug with a reasoned error. | `build.md:198,437,506-507`; `contributing.md:102,431,26,356`; `security.md:17`; `architecture.md:423-433`; `scripts/build.bat:33-44`; `scripts/build.sh:36-47` |
| **A-23** | Packaging/CI hygiene: REUSE.toml dead paths (`deskflow-client|server` manifests, `welcome.png`, `deskflow*.svg` pattern vs actual `org.deskflow.deskflow*.svg`); `build.md` claims "no step consumes the Flatpak manifest" while `ci.yml` has `build-flatpak`; `delivery.md` "only DEB and RPM" omits flatpak + Arch PKGBUILD; WiX triangle CI=5.0.2 / CPack=4 / docs=v7; hardcoded `/Applications/Xcode_16.4.app` macos-x64; vcpkg action `revision: master` unpinned; sonar excludes `subprojects/**` and `src/apps/synergy-gui/**` (both nonexistent) while missing `extra/`. | `REUSE.toml:20-22,36-37`; `build.md:182-186,426`; `delivery.md:88-92`; `install-dependencies/action.yml:144-147`; `deploy/windows/deploy.cmake:37`; `delivery.md:31`; `ci.yml:296`; `install-dependencies/action.yml:122`; `config/sonar-project.properties:6-7` |
| **A-24** | Upstream-identity residue in shipping surfaces: `Synergy App Ltd` SPDX headers (5 files); metainfo release URL → `symless.com`; `AboutDialog.ui:108` notr fallback "Deskflow" (visible if logo fails) + hidden contributors link to deskflow repo; `daemonName()` returns "Deskflow Client/Server" fed to Windows SystemLogger (event-log source name — verify on a machine); manpage SEE ALSO → deskflow wiki; issue-template `config.yml` contact_links all point at deskflow discussions/Matrix with "things you have done with Deskflow" copy. | listed paths; `ClientApp.cpp:98-103`; `ServerApp.cpp:581-586`; `AppUtilWindows.cpp:91`; `src/apps/res/manpage.txt:1-5`; `.github/ISSUE_TEMPLATE/config.yml` |
| **A-25** | U-15 residue: `metainfo.xml:7` `project_license` and `com.tupig.synergy.yml:2` SPDX are bare `GPL-2.0-only`, missing `WITH LicenseRef-OpenSSL-Exception`. | those lines vs `vcpkg.json:8` |
| **A-26** | A-10 tracker row stale (code closed in `cbd2baf39`; `:78` still 挂账). One-line status flip + changelog row. | tracker `:78` vs `IDataSocket.cpp` |
| **A-27** | Light icon theme lacks `places/64/user-trash`: dark alias is in `synergy.qrc`, the light SVG exists on disk but is in no qrc and `synergy-light.theme` has no `places/64` directory → Windows light mode delete button has no icon. | `extra/src/apps/res/synergy.qrc:90`; `synergy-light.theme:15`; `src/apps/res/icons/deskflow-light/places/64/user-trash.svg`; consumer `ServerConfigDialog.cpp:43` |

#### Open U items (after `1269e7cb9`)

| ID | Reality | Doc debt |
|---|---|---|
| U-07 | **Fixed** — GUI links only `synergy.qrc` (`deskflow-gui/CMakeLists.txt:41`); D1 locked in plan-B | detail `:170` still says open; D1 not copied into delivery/AGENTS |
| U-09 | **By design** — documented in contributing `:314-316`; E2 locked | detail `:184` still "Decision needed" |
| U-17 | **Fixed** — canonical `TuPig Synergy` in desktop/metainfo/README/cmake | detail table `:250-257` still lists four names incl. `TuPig Synergy 1` (gone) |
| U-18 | **Fixed (hardened)** — `.ts` names from `UPSTREAM_PROJECT_NAME`; tests use `kUpstreamId` | detail `:263` cites stale line numbers; residual literal `deskflow_en.qm` at `I18NTests.cpp:145` |
| U-19 | **By design** — rule documented in contributing `:299-312` | detail `:267-270` still says "inconsistent" |
| U-21 | **Recorded** — Linux DnD never implemented; Win+mac scope in delivery | **README `:42` says the whole feature is unimplemented (reverse-stale = A-16)** |

No new product decision is required for U-07/09/17/18/19 — only ledger sync (folded into A-19) plus A-16/A-27.

### Consolidated findings

| ID | Sev | Summary | Register |
|---|---|---|---|
| A-12 | P1 | CI gate green with zero builds (lint-clang cwd + ci-passed needs + skipped=ok) | new |
| A-13 | P1 | build-flatpak `uses:` paths anchored to wrong root | new |
| A-14 | P2 | push→beta only; static-analysis never invoked | new |
| A-15 | P2 | Transport factory unused; 3-level rollback claim false | new (vs HANDOFF) |
| A-16 | P2 | README drag-drop status reverse-stale | new (U-21 neighbor) |
| A-17 | P2 | README config example keys do not exist | new |
| A-18 | P2 | HANDOFF drift recurrence (A-03/U-14) | reopen A-03 / U-14 |
| A-19 | P2 | Tracker changelog + U detail sections out of sync (A-04/A-05 recurrence) | reopen A-04 / A-05 |
| A-20…A-27 | P3 | GoogleTest ghost; CMake batch; doc dead-links; packaging hygiene; identity residue; U-15 residue; A-10 row; light trash icon | new |

**R8 risk (carried forward)**: macOS/Linux delivery still has zero machine verification (`delivery.md:25-26` self-declared).

### Remediation plan

T = 2026-09-24. Roles: **Dev** decides, **Agent** executes (propose-first per AGENTS).

#### Tier 1 — P1, due T+1

| ID | Smallest correct change | Verification |
|---|---|---|
| A-12 | Give `lint-clang` job (or the action steps) `working-directory: synergy`; add `lint-clang` (and optionally `analyze-valgrind`) to `ci-passed` needs; keep `ok()` semantics but never let a failed lint become success. | Live run or `act` dry-run: lint finds `src/`; intentionally break format → `ci-passed` fails |
| A-13 | Anchor flatpak explicitly: set `working-directory` on the `uses:` step if supported, or prefix `manifest-path` with `synergy/`, and align Validate/Upload to one root (prefer workspace root + `synergy/` prefixes consistently). | Live `build-flatpak` green on a PR |

#### Tier 2 — P2, due T+3

| ID | Action |
|---|---|
| A-14 | `push.branches: [main]`; either call `static-analysis.yml` from ci.yml or delete the false header comment and document manual runs |
| A-15 | Either wire factory into ServerApp/ClientApp (plan-B Phase 1) **or** rewrite HANDOFF rollback section to match reality (recommend the latter as docs-first) |
| A-16 | Rewrite README `:42` from delivery matrix (Win/mac Implemented, Linux not) |
| A-17 | Replace README config block with real keys from `Settings.h` / point to `configuration.md` |
| A-18 | Refresh HANDOFF header commit, latest-6 block, U-count, and reconcile `:6` vs `:74`/`:185` |
| A-19 | Changelog backfill 6 commits; add Resolution paragraphs to U-07/09/10/13/15/17 detail sections; archive `:63`; flip A-10 row (A-26) |

#### Tier 3 — P3, due T+7

A-20 docs swap to Qt Test · A-21 CMake fixes (each a one-liner, one commit for the batch) · A-22 dead-link sweep · A-23 REUSE/build/delivery/WiX/Xcode/vcpkg-revision/sonar · A-24 identity residue (Dev: keep SPDX attribution vs rebrand — decide once for all five files) · A-25 license strings · A-27 add light trash icon to qrc + theme.

---

## 中文

### 第 0 轮 — 方法

四路并行只读取证，随后对每条 P1/P2 结论独立复核：

| 路 | 焦点 |
|---|---|
| 1 | A-01…A-11 与 U-01…U-21 回归扫 |
| 2 | R1–R10 合规 + 跨文档对账 |
| 3 | 构建 / CI / 源码 / 交付（新发现原 `N-nn` → 改号 `A-nn`） |
| 4 | 剩余 Open 项深挖 + 同类扫新（原 `C-n`） |

基线：工作区干净；HEAD `8695555be`。

### 第 1 轮 — 昨日关闭项回归

| ID | 判定 | 证据 |
|---|---|---|
| A-01 | **成立** | presets minor=25；`3.24` 仅剩 RHEL floor（已记录决策） |
| A-02 | **成立** | `REQUIRED_QT_VERSION=6.7.0`，无 6.4.0 残留 |
| A-06 / A-08 / U-08 / A-11 | **成立** | 预设提示、注释、链接均正确 |
| U-01…U-06、U-16 身份、U-20 | **成立** | 产物名、打包残留、mac 图标、PKGBUILD、CI 身份、install-service 否定表述 |
| **A-03 / U-14** | **回归** | HANDOFF 头部 commit 落后 7 笔；「最新 6 个」与真实不符；`:128` 写 19 项与 `:73` 的 21 项打架 |
| **A-04** | **部分** | §2.3 已修；变更日志缺 `d8512e321` 之后 6 笔（含两笔关闭提交） |
| **A-05** | **部分** | 汇总表已修；详情节仍停在修复前（U-07 `:170`、U-09/10 `:184/:190`、U-17 表 `:250-257`、U-10 引用已删路径、`:63` 未归档） |
| **U-10** | **部分** | developer/homepage 已改；残留 `Synergy App Ltd` SPDX ×5、metainfo `:82` symless 下载链 |
| **U-15** | **部分** | vcpkg/README/PKGBUILD 已齐；metainfo `:7` 与 flatpak yml `:2` 缺 OpenSSL exception |
| **A-10** | **仅追踪过期** | 代码已 throw；追踪表 `:78` 仍「挂账」 |

**成立 13 · 回归 1 · 部分 4 · 过期 1。** 代码修复未被破坏；今日失败面集中在文档同步债。

### 第 2 轮 — R1–R10

十条全部 **通过**。要点：全仓仅 4 个 `.bat`（纯 ASCII、`%~dp0` 自定位）；baseline 单源；`vendor/`/`build/` 未入库；平台目录齐备；双语文档除 U-11 已记录的 3 份操作类文件外合规。R2 边角：`setup.bat` 失败路径 `pause` 交互；`ci-windows-signing.bat` 用 cwd 相对路径。

### 第 3 轮 — 新发现（A-12 … A-27）

#### P1

| ID | 问题 | 证据 |
|---|---|---|
| **A-12** | **CI 可以「全绿但零构建」。** `lint-clang` composite 无 `working-directory`，`find src/` 在仓库根执行（根下无 `src/`）→ 失败；所有 build job `needs: lint-clang` → 跳过；`ci-passed` 不 needs `lint-clang` 且 `ok()` 把 `skipped` 当成功 → 分支保护绿灯、实际零构建。 | `lint-clang/action.yml:12`；`ci.yml:71,79-92,106,131,271,424,648` |
| **A-13** | **build-flatpak 路径锚定互相矛盾。** `defaults.run` 只作用于 `run` 步骤；`uses: flatpak-builder` 的 `manifest-path: extra/...` 以 workspace 根为基准（清单在 `synergy/extra/...`）。Validate 在 `synergy/` 下找 `repo`，Upload 期望 `${workspace}/synergy/*.flatpak` —— 三者至少两处错位。*待线上验证。* | `ci.yml:63-65,674-695` |

#### P2

| ID | 问题 | 证据 |
|---|---|---|
| **A-14** | `push` 只配不存在的 `beta` 分支 → main 推送永不触发 CI；`static-analysis.yml` 无任何调用者，clang-tidy/cppcheck 从未自动运行，头注释「Triggered by ci.yml」失实。 | `ci.yml:33-34`；`static-analysis.yml:2,6-8` |
| **A-15** | `NetworkTransportFactory` 无生产调用者，实际永远走 legacy。HANDOFF 宣称的 3 级回滚落空：`USE_LEGACY_NETWORK` 只在未接线的工厂里读；CMake `LEGACY_NETWORK` 选项全仓不存在。plan-B `:27` 自认 unused，与 HANDOFF `:102` 矛盾。 | `NetworkTransportFactory.cpp:32-41`；`HANDOFF.md:102` vs `plan-B:27` |
| **A-16** | README 称文件拖拽「未实现」—— 与交付矩阵（Win/mac 已实现 + 单测）**反向过期**。 | `README.md:42` vs `delivery.md:106-111` |
| **A-17** | README 配置示例键全不存在（`serverHost/serverPort/reconnectInterval/autoConnect`）；真实键为 `client/remoteHost`、`core/port` 等；`[server] screens=` 是服务端配置文件语法。照抄必失效。 | `README.md:166-186` vs `Settings.h:37,43,51` |
| **A-18** | **A-03/U-14 回归。** HANDOFF 头部/提交列表过期；U 计数 19 vs 21；`:6` 称身份债已关 vs `:74`/`:185` 仍列 U-07/09 待决策（台账汇总早已 Fixed/By design）。 | `HANDOFF.md:5,6,74,128,185` |
| **A-19** | **A-04/A-05 回归。** (a) 追踪变更日志缺后 6 笔（含 `cbd2baf39`、`1269e7cb9` 两笔关闭）；(b) U 台账详情节与状态栏矛盾（行号见第 1 轮）。 | 追踪 §5；`consistency-audit.md` 各详情行 |

#### P3

| ID | 问题 |
|---|---|
| **A-20** | GoogleTest 幽灵声明 ×7 处，实际为 `Qt::Test` |
| **A-21** | CMake 缺陷批：xkbfile `!` 死检查、多配置下 NDEBUG 误加、`generate_app_man` 变量大小写、`PORTABLE_LIBS` 死语句、`SKIP_BUILD_TESTS` 双源矛盾、libportal/libei 版本文档不符、`build.sh:17` 残句 |
| **A-22** | 文档死链/幽灵：mingw-toolchain 不存在、`.pre-commit-config.yaml` 不存在、`bug_report.md` 应为 `.yml`、security.md 无邮箱、ADR-0011 目录树过期、`build.md` 声称支持 `debug` 参数但脚本拒绝 |
| **A-23** | 打包/CI 卫生：REUSE 死路径 ×4、build.md「无人消费 Flatpak」与 CI job 矛盾、delivery 漏 flatpak+Arch、WiX 5/4/7 三角、Xcode 绝对路径、vcpkg `revision: master` 未钉、sonar 幽灵排除 |
| **A-24** | 上游身份残留：`Synergy App Ltd` SPDX ×5、symless 下载链、AboutDialog「Deskflow」回退文本、`daemonName()` 上游名进事件日志、manpage 指上游 wiki、issue `config.yml` 全指 deskflow |
| **A-25** | U-15 残留：metainfo/flatpak SPDX 缺 OpenSSL exception |
| **A-26** | A-10 追踪行过期（一行状态 + 变更日志） |
| **A-27** | 亮色主题缺 `places/64/user-trash`（Windows 亮色删除按钮无图标） |

#### Open U 项现状（`1269e7cb9` 之后）

U-07/17/18 已修、U-09/19 有意设计、U-21 已记录 —— **均无需新决策**；欠的只是详情节同步（并入 A-19）与 A-16/A-27 两处清理。

### 发现汇总

| ID | 级别 | 摘要 |
|---|---|---|
| A-12 / A-13 | P1 | CI 门禁可空转绿灯；flatpak 路径锚定错位 |
| A-14…A-19 | P2 | 触发分支与静态分析失效；工厂未接线/回滚承诺落空；README 两处反向/错误；HANDOFF 与追踪台账同步债回归 |
| A-20…A-27 | P3 | GoogleTest 幽灵；CMake 批；死链；打包卫生；身份残留；许可残留；A-10 行；亮色图标 |

**R8 风险（沿革）**：macOS/Linux 交付仍无实机验证（`delivery.md:25-26` 自述）。

### 整改计划

T = 2026-09-24。

| 层 | 项 | 动作要点 | 期限 |
|---|---|---|---|
| **P1** | A-12 | lint 补 `working-directory: synergy`；`ci-passed` 纳入 lint needs | T+1 |
| **P1** | A-13 | flatpak 三处路径统一到同一根 | T+1 |
| **P2** | A-14 | push→main；接入或澄清 static-analysis | T+3 |
| **P2** | A-15 | 接线工厂 **或** 先改 HANDOFF 回滚描述为现实 | T+3 |
| **P2** | A-16/A-17 | README 拖拽状态与配置示例按权威源重写 | T+3 |
| **P2** | A-18/A-19 | HANDOFF 刷新；变更日志补 6 笔；U 详情节补 Resolution；A-10 行翻转 | T+3 |
| **P3** | A-20…A-27 | 各为小改，分批提交；A-24 版权策略需 Dev 一次拍板 | T+7 |
